{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Iniciar Gestão de Projetos (servidor)",
      "type": "node-terminal",
      "request": "launch",
      "command": "iniciar-servidor.bat",
      "cwd": "${workspaceFolder}/server"
    }
  ]
}
